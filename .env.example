# No API keys required — map uses OpenStreetMap (free, no token needed)
# Image generation uses Pollinations.ai — free, no key needed
# Free at console.groq.com — no credit card needed
VITE_GROQ_API_KEY=your_groq_key_here
